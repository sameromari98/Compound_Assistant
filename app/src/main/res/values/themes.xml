<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- App theme used by AndroidManifest -->
    <style name="Theme.CompoundAssistant" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Optional polish -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <!-- If you want Material3 colors in Compose, keep using MaterialTheme in code.
             This XML theme is just for framework window/chrome. -->
    </style>
</resources>